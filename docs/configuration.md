# Configuration

All configuration settings are in `app/config.py`. Environment variables are loaded using `python-dotenv`.

Example of a `.env` file:

```
SECRET_KEY=your_super_secret_key
DEBUG=True
```

Make sure the file `.env` is placed in the project root.